<UserControl x:Class="ZMK.Wpf.Views.Controls.StatusPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ZMK.Wpf.Views.Controls" 
             xmlns:viewmodels="clr-namespace:ZMK.Wpf.ViewModels" 
             d:DataContext="{d:DesignInstance Type=viewmodels:StatusPanelViewModel, IsDesignTimeCreatable=True}"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <TextBlock Text="{Binding CurrentUserInfoRaw}" 
                   HorizontalAlignment="Left" 
                   VerticalAlignment="Center" 
                   Margin="5"/>

        <TextBlock Text="{Binding LoginDateRaw}" 
                   HorizontalAlignment="Right" 
                   VerticalAlignment="Center" 
                   Margin="5" 
                   Grid.Column="1"/>
    </Grid>
</UserControl>
